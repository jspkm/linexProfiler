{
  "name": "linex-terminal-workspace",
  "private": true,
  "scripts": {
    "dev:check": "bash scripts/dev-check.sh",
    "dev:all": "npm run dev:check && bash -lc 'trap \"kill 0\" EXIT INT TERM; (cd backend && conda activate linex-terminal && python dev_server.py) & (cd web && npm run dev) & wait'"
  }
}
